{% extends "base.html.twig" %}

{% block title %}Page à propos{% endblock %}

{% block body %}

<section id="about" class="section-fix">
    {% for about in about %}
    <div class="container">
        <div class="row">
            <div class="col-lg-12 mb-5">
                <h1 class="text-center">{{ about.title }}</h1>
            </div>

            <div class="col-lg-6 m-auto col-img-fix">
                <img src="{{ about.image }}" alt="Autoportrait" class="img-fluid">
            </div>
            <div class="col-lg-6 my-auto">
                <p>{{ about.presentation|raw }}</p>
            </div>
        </div>
    </div>
    {% endfor %}
</section>
   
{% endblock %}