<div class="container">
    <h1>Nouveau message depuis le site MAD STUDIO</h1>
    <p>{{ contact.lastname }}</p>
    <p>{{ contact.firstname }}</p>
    <p>{{ contact.compagny}}</p>
    <p>{{ contact.phone }}</p>
    <p>{{ contact.mail }}</p>
    <p>{{ contact.message }}</p>
</div>
