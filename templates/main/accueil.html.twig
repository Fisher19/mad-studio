{# {% if app.request.hasPreviousSession %}
    {% for message in app.flashes('notice') %}
        <div class="flash-notice">
            {{ message }}
        </div>
    {% endfor %}
{% endif %} #}

<section id="accueil" class="section section1">
    <div class="container">
        <div class="row" id="accueil-animate">
            <div class="col-md-7">
                <h1>Photographe et créatrice de site web
                </h1>
            </div>
        </div>
    </div>
</section>
