<section id="arguments" class="section section5">
	<div class="container pt-5">
  		<div class="mx-md-5">
    		<div class="row text-center">
      			<h1 class="pb-4">Artisan, créateur, commerçant, producteur</h1>
				<p class="mx-auto">Je vous accompagne pour développer la visibilité de votre entreprise sur internet.</p>
    		</div>
    		<div class="row text-center mt-4">
			{% for argument in arguments %}
      			<div class="col-lg-3 col-md-6 mb-lg-0 mb-5">
        			<img src="{{ vich_uploader_asset(argument, 'iconFile') }}" class="icon-argument" alt="...">
        			<h3 class="mb-4">{{ argument.title }}</h3>
        			<p class="px-2 mb-lg-0">{{ argument.content|raw }}</p>
      			</div>
			{% endfor %}
			</div>
		</div>
	</div>
</section>

