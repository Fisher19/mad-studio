<section id="arguments" class="section section5">
	<div class="container my-5 p-3">
    		<div class="row text-center">
      			<h1 class="mx-auto my-5">Pourquoi choisir MAD STUDIO ?</h1>
    		</div>
    		<div class="row">
			{% for argument in arguments %}
      			<div class="col-lg-6 col-md-12 mt-3 px-5">
        			<h3><i class="fas fa-check"></i> {{ argument.title }}</h3>
        			<p>{{ argument.content|raw }}</p>
      			</div>
			{% endfor %}
			</div>
			<div class="text-right">
				<h4>Découvrez l'histoire de MAD STUDIO </h4>
                <a class="" href="{{ path('about_index') }}" role="button">A propos</a>
            </div>
		</div>
</section>

