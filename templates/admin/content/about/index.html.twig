{% extends 'admin/base.html.twig' %}

{% block title %}Gestion de A propos{% endblock %}

{% block body %}
    <div class="container p-5">
        <h4>Gérer A propos</h4>

        {% for message in app.flashes('success') %}
            <div class="alert alert-success">
                {{ message }}
            </div>
        {% endfor %}
            <table class="table table-sm table-hover table-bordered">
                <thead class="thead-dark text-center">
                    <tr>
                        <th>Contenu</th>
                        <th>Actions</th>
                    </tr>
                </thead>
                <tbody>
                {% for about in about %}
                    <tr>
                        <td class="align-middle">{{ about.presentation|raw }}</td>
                        <td class="align-middle">
                            <a href="{{ path('admin_about_edit', {id: about.id}) }}" class="btn btn-dark">
                                <i class="fas fa-edit my-1"></i>
                            </a>                       
                        </td>
                    </tr>
                {% endfor %}
                </tbody>
            </table>

    </div>
{% endblock %}
