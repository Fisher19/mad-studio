{% extends 'admin/base.html.twig' %}

{% block body %}

    <div class="container-fluid p-5 h-100 bg-success">
        <h2>Editer l'argument : "{{ argument.title }}"</h2>

        {{ form_start(form) }}
            {{ form_rest(form) }}
            <button class="btn btn-primary">{{ button|default('Enregistrer') }}</button>    
        {{ form_end(form) }}
        
    </div>

{% endblock %}